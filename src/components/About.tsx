import { Title } from "src/components/shared/Title";

export const About = () => {
  return (
    <div className="bg-black text-white min-h-screen">
      <Title title={"ABOUT"} />
    </div>
  );
};
